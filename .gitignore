# Godot 4 generated cache — regenerate with: godot --headless --import
.godot/

# Fetched from the asset server (srv) — see fetch_assets.sh
assets/meshes/
# Gallery screenshots from future/tests/harnesses/plane_gallery.tscn
.screenshots/
